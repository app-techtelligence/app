/**
 * Message namespaces of the service-detail pages (§4 site map:
 * /consulting/ai and /consulting/data-governance). The section components
 * in this folder are shared between both pages and driven by namespace.
 */
export type ServiceNamespace = "serviceAi" | "serviceDataGovernance";
